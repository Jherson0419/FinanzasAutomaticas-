<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fase 56: image_picker necesita esto para abrir la galería. API 33+
         (Android 13) usa READ_MEDIA_IMAGES; en versiones anteriores,
         READ_EXTERNAL_STORAGE (limitado con maxSdkVersion para que el
         sistema no lo pida de más en dispositivos nuevos). -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <application
        android:label="Finzo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Fase 54: deep link de confirmación de correo (finzo://login-callback),
                 usado como `emailRedirectTo` al crear una cuenta — supabase_flutter
                 lo intercepta solo para resolver la sesión. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="finzo" android:host="login-callback"/>
            </intent-filter>
            <!-- Fase 64: deep link para compartir el perfil (finzo://agregar-amigo?nick=),
                 generado por "Compartir mi perfil" en mi_perfil_screen.dart. A diferencia
                 del de arriba, este lo escucha la propia app (app_links, ver app.dart),
                 no supabase_flutter. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="finzo" android:host="agregar-amigo"/>
            </intent-filter>
            <!-- Fase 65: deep link de recuperación de contraseña (finzo://reset-password),
                 usado como `redirectTo` en `resetPasswordForEmail` — igual que
                 login-callback, supabase_flutter lo intercepta para armar la sesión de
                 recuperación (AuthChangeEvent.passwordRecovery). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="finzo" android:host="reset-password"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
